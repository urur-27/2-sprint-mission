spring:
  application:
    name: discodeit
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

discodeit:
  repository:
    type: jcf   # jcf | file
    file-directory: output/
